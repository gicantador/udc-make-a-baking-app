<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pgcn.udcmakeabaking"
    android:installLocation="preferExternal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity
            android:name="com.pgcn.udcmakeabaking.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="com.pgcn.udcmakeabaking.data.BakingContentProvider"
            android:authorities="com.pgcn.udcmakeabaking.data"
            android:exported="false" />

        <provider
            android:name="com.pgcn.udcmakeabaking.data.IngredientsContentProvider"
            android:authorities="com.pgcn.udcmakeabaking.data"
            android:exported="false" />

        <provider
            android:name="com.pgcn.udcmakeabaking.data.StepsContentProvider"
            android:authorities="com.pgcn.udcmakeabaking.data"
            android:exported="false" />
    </application>


</manifest>